# Helios Batch Input Switcher
Megapixel Helios Batch Input Switcher: a simple tool to cycle between inputs of a batch of Helios LED processors.
Written to a DisplayPort model scenario.
For different input models, update the payload by replacing dp1/dp2 with the appropriate input name as specified in the API documentation.
